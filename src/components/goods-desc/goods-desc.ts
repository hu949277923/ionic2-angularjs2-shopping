import { Component/*, Input*/ } from '@angular/core';

@Component({
  selector: 'cm-goods-desc',
  templateUrl: 'goods-desc.html'
})
export class goodsDescComponent {
  // @Input()
  constructor() {
    //console.log(TabsPage);
  }
}
